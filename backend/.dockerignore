# Ignore node_modules and local builds
node_modules
npm-debug.log
dist
build

# Ignore uploads (they’re mounted via volume)
uploads

# Ignore Git and Docker files
.git
.gitignore
Dockerfile
docker-compose.yml

# Ignore environment files
.env
*.env
